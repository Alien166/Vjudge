#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,k,m=0; cin>>n>>m;
    for(int i=0;i<m;i++)
    {
        k=n%10;   //last digits
        if(k==0)
        {
            n /=10;
        }
        else
            n -=1;
    }
    cout<<n;

    return 0;
}
